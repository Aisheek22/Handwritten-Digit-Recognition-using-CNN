# Handwritten-Digit-Recognition-using-CNN
This project implements a Handwritten Digit Recognition system using a Convolutional Neural Network (CNN) to classify digits (0–9) from grayscale images. The model is trained on the MNIST Dataset, a widely used benchmark dataset in computer vision.  
